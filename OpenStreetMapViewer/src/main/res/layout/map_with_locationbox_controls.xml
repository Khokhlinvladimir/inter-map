<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/parent_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent" >

    <org.osmdroid.views.MapView
        android:id="@+id/mapview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tilesource="Mapnik" />

    <TextView
        android:id="@+id/textViewCurrentLocation"
        android:layout_width="fill_parent"
        android:text="PLACEHOLDER"
        android:textAlignment="center"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:background="#77000000"
        android:layout_height="wrap_content" />

    <ImageButton
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_material_add"
        android:contentDescription="@string/bt_zoom_in_description"
        android:layout_below="@id/textViewCurrentLocation"
        android:layout_alignParentStart="true"
        android:id="@+id/btnRotateLeft"
        />

    <ImageButton
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_material_remove"
        android:contentDescription="@string/bt_zoom_out_description"
        android:layout_below="@id/textViewCurrentLocation"
        android:layout_alignParentEnd="true"
        android:id="@+id/btnRotateRight"
        />

</RelativeLayout>
